{
  "name": "career-os",
  "version": "1.0.0",
  "description": "CareerOS is a single-user job application operating system for tracking companies, applications, resume versions, job descriptions, contacts, interviews, reminders, and analytics.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "bench:dashboard": "node benchmarks/dashboard.mjs",
    "bench:mixed": "k6 run benchmarks/k6/mixed-workload.js",
    "bench:search": "k6 run benchmarks/k6/search.js",
    "build:api": "npm run build --prefix backend",
    "dev:api": "dotenv -e .env -- npm run dev --prefix backend",
    "dev:wait-api": "node scripts/wait-for-api.mjs",
    "dev:worker": "dotenv -e .env -- npm run dev:worker --prefix backend",
    "infra:up": "docker compose up -d --wait postgres redis",
    "format:check:api": "npm run format:check --prefix backend",
    "lint:api": "npm run lint --prefix backend",
    "migrate:down": "dotenv -e .env -- npm run migrate --prefix backend -- down",
    "migrate:status": "dotenv -e .env -- npm run migrate --prefix backend -- status",
    "migrate:up": "dotenv -e .env -- npm run migrate --prefix backend -- up",
    "seed": "dotenv -e .env -- npm run seed --prefix backend",
    "test:api": "npm run test --prefix backend",
    "typecheck:api": "npm run typecheck --prefix backend",
    "predev": "node scripts/ensure-dev-infra.mjs && npm run migrate:up",
    "dev": "dotenv -e .env -- concurrently --kill-others-on-fail --names \"api,worker,web\" --prefix-colors \"cyan,yellow,magenta\" \"npm run dev --prefix backend\" \"npm run dev:wait-api && npm run dev:worker --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tiongMax/career-os.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/tiongMax/career-os/issues"
  },
  "homepage": "https://github.com/tiongMax/career-os#readme",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv-cli": "^11.0.0"
  }
}
